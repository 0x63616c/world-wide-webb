name: world-wide-webb-cloudflare
runtime:
  name: nodejs
  options:
    typescript: false
description: >
  world-wide-webb Cloudflare edge state (www-j934.2): adopt-only Access apps +
  tunnel ingress + DNS, a separate Pulumi project from the k8s foundation.
main: program.ts
