{
  "name": "@control-center/storybook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd ../web storybook",
    "build": "bun run --cwd ../web build-storybook",
    "test": "bun run --cwd ../web test",
    "typecheck": "bun run --cwd ../web typecheck"
  }
}
