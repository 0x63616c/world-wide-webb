import { router } from "../init";

// Media router — foundation for Apple TV, Sonos, and Spotify queries/mutations.
// Procedures are added by subsequent milestone features; this skeleton registers
// the router on the app router so typecheck sees it as part of AppRouter.
export const mediaRouter = router({});
