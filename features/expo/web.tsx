import { Tile } from "@/components/ui";

export function ExpoTile() {
  return (
    <Tile style={{ alignItems: "center", justifyContent: "center" }}>
      <span style={{ color: "var(--ink)", fontSize: 30, fontWeight: 700 }}>expo</span>
    </Tile>
  );
}
