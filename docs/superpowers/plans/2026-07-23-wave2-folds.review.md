# Wave 2 Folds — Wave-Boundary Review

**Commits:** tesla 4c4cd22c5 · dogcam dd32ef393 · weight 69d293a88 · deploys cd9fe76e2
**Verdict: CLEAN** (safe to proceed to Wave 3)
**Counts:** BLOCKER 0 · MAJOR 0 · MINOR 3

## What was verified

1. **Fold completeness** — Each of `features/{tesla,dogcam,weight,deploys}/` has manifest.ts + web.tsx (view is the file, old `*Tile.tsx` wrapper deleted) + api.ts + service.ts + (weight/deploys) schema.ts + db.ts, plus co-located tests. All old files confirmed DELETED: `apps/web/.../{TeslaTile,DogCamTile,WeightTile,DeployTile}.tsx`, routers `{tesla,camera,weight,github}.ts`, services `{tesla-service,weight-domain,weight-sql,github-actions-service}.ts`. No survivors.

2. **Registry → manifest** — `apps/web/src/lib/tile-registry.ts` no longer holds inline coord entries for these tiles; it imports the four manifests and pushes them into the registry array. `features/_generated/{router,tiles,schema}.gen.ts` include all four (routers merged; tiles tile_tesla/tile_dogcam/tile_weight/tile_deploys; schema re-exports weight + deploys). `bun run apps:check` reports **clean — no drift**.

3. **Boundary rule (critical)** — `bunx biome lint features/{tesla,dogcam,weight,deploys}` = 36 files, **clean**. Grep for `apps/api` imports across the four features returns only prose comments (pointer notes like "never apps/api", "see apps/api/src/server.ts"), ZERO real imports. The features→apps/api noRestrictedImports rule is green. This is the migration's whole point and it holds.

4. **Worker intervals correctly left hand-wired (S1 not built)** — No `jobs.ts` in weight or deploys. weight-ingest stays in `apps/api/src/services/weight-service.ts`, now importing `@features/weight/schema` + `@features/weight/service` (allowed direction) and still using apps/api's shared `db` pool. deploys github-poll: `runGithubPollCycle` re-exported from `@features/deploys/service` via `apps/api/src/worker-deps.ts` (moved off the deleted github-actions-service). Both remain interval cycles, not queue jobs. Correct.

5. **dogcam camera stream (S3 not built)** — Raw HTTP route `/media/camera-stream` still lives in `apps/api/src/server.ts` (server.ts:143), now calling `openCameraStream` imported from `@features/dogcam/service` (allowed direction). Not half-moved, not broken.

6. **placeholder-tiles / bento clearance** — `apps/web` `placeholder-tiles.test.ts` = 4 tests PASS. Gap-free tiling intact.

7. **knip** — clean; only two config hints (`.claude/**`, `.opencode/**` ignore entries), no dead exports. The weight unused-`pool` export noted in the plan is resolved (only remaining `export const pool` is the legitimate shared `apps/api/src/db/index.ts` singleton).

## Findings

**[MINOR] Global `bun run lint` is red, but not from these folds.** 13 errors live in `infra/esphome/.esphome/storage/*.yaml.json` (build-cache formatting), `apps/web/.../GuestWifiDesignSheet.stories.tsx` (noUselessFragments), and `apps/web/src/portal/__tests__/bundle-isolation.test.ts` (noTemplateCurlyInString). All last touched by 1510657bc (layout-adopt commit), none by the four fold commits; the four features lint clean in isolation. Pre-existing debt — worth a cleanup pass but does not block Wave 3.

**[MINOR] weight opens a second Postgres pool against `weight_measurement`.** `features/weight/db.ts` opens its own pool (guest-wifi precedent) while the untouched ingest cycle keeps using apps/api's shared pool — two pools against the same table from one process tree. Intentional and documented in the commit body; acceptable at this stage but flag for the eventual @www/core db consolidation so it doesn't multiply across every folded feature.

**[MINOR] test-storybook CI runtime (tesla run hit 15m timeout).** Not caused by the folds: each fold's stat shows the `*TileView.stories.tsx` change is `2 +-` — a one-line import repoint, no net-new stories (repo total 132). Story count is flat across the wave. The timeout is a standing infra concern (browser-per-story, already near the ceiling at 132), not a fold regression. **Severity: low-for-this-wave; mitigation = WATCH, not now.** Recommend sharding/parallelizing test-storybook before a later wave folds a tile that genuinely adds stories, so headroom exists.

## No slop observed
No half-done repoints, no duplicated logic between features/ and apps/api (services were moved, not copied — deletes confirmed), no weakened or skipped tests (test files moved with `git mv`-style renames + assertion counts preserved; dogcam/weight/tesla tests co-located and passing paths intact), no raw cross-feature imports (feature-to-feature goes through generated router).
