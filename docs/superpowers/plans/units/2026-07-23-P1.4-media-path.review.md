# Review — P1.4 hoist `media-path` to `@www/core`

**Verdict: APPROVE-WITH-FIXES** (0 BLOCKER, 0 MAJOR, 3 MINOR)

Independent verification against real code. The plan's core is sound: `media-path.ts`
is genuinely pure (`node:fs/promises` + `node:path` only, no db/env/apps-api imports —
confirmed by reading all 138 lines), exports only named pure functions, has no
env-bound singleton, and its consumers all sit inside `apps/api`. The no-shim /
repoint-directly decision is correct — a re-export shim would be dead indirection and
knip would flag it. The git-mv-and-repoint mechanics, barrel insertion point, and
verify chain are accurate.

## Verified facts (plan claims that hold)

- **"3 importers" count is CORRECT.** Repo-wide grep over `apps features packages
  scripts` finds exactly three production consumers, all in `apps/api/src/services/`:
  `wake-photo-service.ts:9`, `booth-photo-service.ts:9`, `photo-path-migration.ts:9`,
  plus the colocated `media-path.test.ts` (moves). No `features/*` importer, no
  `@www/api`/`@features`-aliased importer, nothing in `features/_generated/*`. The
  only other hits are doc-comment mentions (booth :16, wake :14) and a stale
  `apps/api/node_modules/.vite/…/results.json` cache (build artifact, not a source
  importer — ignore).
- **`apps/api/package.json` declares `@www/core`** — `"@www/core": "workspace:*"` at
  line 28 (PLACEHOLDER in Open Questions RESOLVED: yes, already present, no
  `bun install` needed). The repointed imports will resolve.
- **`packages/core` will accept `src/media-path/` + barrel with no config change** —
  `tsconfig.json` `include` is `["src", "test"]` (line 6); `package.json` has
  `"test": "vitest run"` and `vitest ^3.2.4` devDep. Moved test is auto-collected.
- **The moved test is self-contained** — imports only `node:fs/promises`, `node:os`,
  `node:path`, `vitest`, and `./media-path`. No apps/api path. The `./media-path` →
  `./index` fix (plan step 3) is the only edit needed. Uses
  `mkdtemp(join(tmpdir(), "media-path-"))` — pure, no external fixture.
- **Boundary claim is not overstated.** `media-path` is used ONLY inside `apps/api`
  today, so P1.4 does not itself break an existing `features/*→apps/api` edge (there
  is none). The plan correctly frames P1.4 as a *prerequisite* for the later
  wake/booth folds, not as the fold itself ("the wake/booth folds happen in later
  units"). No false "unblocks" claim.
- **Barrel insertion point is correct** — real `packages/core/src/index.ts` matches
  the plan's "resulting file" exactly; `./media-path` slots alphabetically between
  `./homeassistant` and `./secrets/hydrate`.

## Findings

1. **[MINOR] Add `photo-path-migration.test.ts` to the verify chain.** It exists
   (`apps/api/src/services/photo-path-migration.test.ts`, confirmed) and directly
   exercises a repointed consumer (`photo-path-migration.ts`, which imports
   `parseLegacyPhotoFileName`/`photoFileName`/`nextFreeName`). The plan's verify chain
   (line 145) names only wake/booth tests speculatively and omits this one. Fix: add
   `bunx vitest run apps/api/src/services/photo-path-migration.test.ts` to the chain.
   Not a correctness risk (typecheck + lint already prove resolution) but it's the
   most direct behavioral test of a repointed consumer, so run it.

2. **[MINOR] Resolve the two PLACEHOLDERs in Open Questions — both answerable from
   code now.** (a) wake/booth tests DO exist and are worth running:
   `wake-photo-service.test.ts`, `booth-photo-service.test.ts` (and
   `wake-photo-purge-service.test.ts`, though it does not import media-path). (b)
   `apps/api/package.json` already declares `@www/core` (line 28) — the "add it if
   missing" branch is dead; drop it. Leaving unresolved PLACEHOLDERs in an
   implementer-facing plan invites a needless grep round-trip.

3. **[MINOR] Doc-comment edits are worthwhile but under-specified on exact text.**
   Plan says rewrite `(see media-path.ts)` → `(see @www/core media-path)` at
   booth :16 and wake :14. Both lines confirmed verbatim. Minor: these are the only
   two source comment references; the plan already lists them, so this is just a note
   to keep them in the explicit staged-paths set (they are — both files are staged for
   the import repoint anyway). No action beyond honoring the plan as written.

## Note (no action)

- knip zero-tolerance is satisfied by construction: the three consumers keep importing
  every exported name (via `@www/core`), so no `@www/core` export goes unused; the old
  file is fully removed by `git mv`. Plan's gotcha framing ("if knip flags unused, a
  consumer was missed") is the correct diagnostic.
- Gotchas (shared-checkout explicit staging, `git pull --rebase --autostash`,
  foreground `gh run watch`, bun-build-CWD-tsconfig) are complete and correct;
  commit message matches the HA-hoist convention (`refactor(core): … (P1.4)`).

**Bottom line:** ship after folding the three MINORs into the plan. No structural
rework needed.
