# Review — P1.6 Relocate photo-path-migration (unweld photo tiles)

**Verdict: APPROVE-WITH-FIXES**

The plan is accurate against real code. Every cited path, line number, symbol, and dependency was verified. The importer list is complete. The shim-vs-repoint decision is correct. The destination reasoning (why not packages/core|platform) holds against real imports. One MINOR (commit scope) and one MINOR (test-count wording) are the only fixes; neither blocks implementation.

## Verification performed

- `apps/api/src/services/photo-path-migration.ts` exists; imports confirmed: `../db/schema` (`boothPhoto`, `wakePhoto`), `./booth-photo-service` (`defaultBoothPhotoRoot`), `./wake-photo-service` (`defaultWakePhotoRoot`), `@www/core`, `@www/logger`, drizzle. All match the plan's File-Structure table and Interfaces block.
- `defaultBoothPhotoRoot` (booth-photo-service.ts:102) and `defaultWakePhotoRoot` (wake-photo-service.ts:60) are real exported functions. Confirmed.
- Whole-repo grep for `migratePhotoPaths | photo-path-migration | photoPathMigration | PhotoPathMigrationResult`: hits ONLY in `server.ts` (import :18, call :68), the module itself, and its test. No other importer. Plan's exhaustive-importer claim is CORRECT.
- No `apps/api/src/services/index.ts` barrel — plan's claim confirmed (nothing re-exports the module).
- `server.ts:18` import and `server.ts:68` call site (`await migratePhotoPaths(db)`) verified verbatim; call site is unchanged by the move. Correct.
- Test file (`photo-path-migration.test.ts`) source imports are exactly `../db/schema` (wakePhoto) and `./photo-path-migration`. No relative import points into `services/`, so it moves without edits — plan's Step 2 claim is CORRECT.
- `apps/api/src/startup/` does not yet exist (new dir, as the plan states). Relative-path arithmetic (both dirs one level under `src/`, `../db/schema` unchanged, `./` service imports become `../services/`) is arithmetically correct.

## Findings

1. **[MINOR] Commit-message scope is wrong.** The proposed message `refactor(core): relocate photo-path-migration ...` uses scope `core`, but this unit touches ZERO files in `packages/core` — it is entirely `apps/api`-internal. The reference commit da0be339e used `core` because it genuinely hoisted into `@www/core`; that does not apply here. Fix: use `refactor(api): relocate photo-path-migration to unweld photo tiles (P1.6)` (no backticks, unchanged otherwise).

2. **[MINOR] Test count wording — "8 tests" is right, guard the language.** Step 4 expects "PASS — 8 tests". Verified: the file has exactly 8 `it(` cases (lines 79, 97, 118, 130, 139, 153, 167, 179). The number is correct; keep it but treat it as informational — vitest's headline count is authoritative, do not fail the step on an off-by-one if the suite is green.

3. **[MINOR / informational] The hoist is apps/api-internal, not a features→apps/api boundary break.** Booth and wake services are still in `apps/api/src/services/` (not yet folded), so this move does not itself sever a Biome-banned dependency today. The plan is honest about this (Architecture: "pure file relocation ... no behavior change"). The value is forward-looking: relocating the welding orchestrator out of `services/` means when booth and wake later fold into separate `features/*`, neither feature carries a module importing the other's service. This is the correct enabling step for the stated goal. No fix — flagged so the implementer/verifier does not expect a lint-boundary delta from this unit. `bun run lint` will pass because it was already passing; it is a no-regression gate here, not a proof-of-unweld.

## Confirmed-correct decisions (no action)

- **No shim / repoint.** Only 2 consumers (server.ts + own test), not an env-bound singleton. A re-export shim at the old `services/` path would be an unused file/export and fail knip's zero-tolerance pass. Matches the P1.4 media-path precedent; correctly distinguished from P1.1 HA (many-consumer singleton kept shims). CORRECT.
- **Destination `apps/api/src/startup/`, not packages/*.** The module imports apps/api-owned `../db/schema` and the two apps/api service helpers; `packages/*` may not import `apps/api` (Biome-banned). packages/core|platform is genuinely ruled out by real deps. `apps/api → features/services` is the allowed direction. CORRECT. The `startup/` name is cosmetic (Open Question) and acceptable as-is.
- **Verify chain** (apps:gen → typecheck → tests → apps:check → knip → lint → rebase → explicit staging → git show --stat) is complete, ordered, and includes the knip zero-tolerance and explicit-path-staging safeguards mandated by AGENTS.md / memory. No gaps.
- **Gotchas** (no `git add -A`, `git pull --rebase --autostash`, no backticks in message, no `_generated` hand-edit, verify `git show --stat`) are all present and correct for this shared-checkout repo.
