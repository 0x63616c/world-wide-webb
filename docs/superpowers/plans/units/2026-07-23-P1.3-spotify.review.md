# P1.3-spotify Plan Review

**Verdict: APPROVE**

Independent verification against real source. Every cited path exists, the importer inventory is complete and exact, the shim-vs-repoint decision is correct, and the verify chain is sound. No blockers. Two minor notes below for the implementer's awareness; neither requires a plan edit.

## Verification performed

- Read the plan, the reference hoist (`packages/core/src/homeassistant/index.ts`, `packages/core/src/index.ts`), and all real Spotify source.
- Repo-wide grep (`integrations/spotify` + every `Spotify*` symbol), worktrees excluded — matched the plan's inventory byte-for-byte.
- Confirmed the three declared non-importers are genuinely non-importers.
- Confirmed dependency declarations, package `exports`, and vitest config support the move.

## Findings

1. **[MINOR] Master plan lists a "playlist poller" importer that does not exist yet — plan correctly omits it, worth a one-line note.** The master plan (`2026-07-23-track-c-master-execution.md:191-193`) says P1.3 should "repoint `spotify-service.ts`, playlist poller, quickplay." A repo-wide grep finds NO playlist poller and NO `features/*` Spotify consumer today — the only real importers are the 4 files the plan lists. The poller/quickplay are future Track-C/S1 work (roadmap `merry-hugging-river.md:158`), and this hoist is the preemptive unblock for them. The plan's inventory is therefore correct and complete; the master-plan wording just refers to consumers that don't exist yet. Fix: none required — optionally add a one-liner to the inventory noting "playlist poller / quickplay do not exist yet; this hoist unblocks them (no importer to repoint today)."

2. **[MINOR] The `features/* -> apps/api` boundary this unblocks is not yet crossed — the hoist is preemptive, not a fix for a current violation.** The unit goal frames the hoist as letting quickplay consume the client without crossing the Biome-banned boundary. Confirmed accurate as *intent*: there is no current `features/*` importer, so `bun run lint` will not newly go green on a boundary violation — it is already green. This does not weaken the plan (the data-layout-first invariant justifies hoisting before the consumer lands), but the implementer should not expect a pre/post lint delta to "prove" the unblock; the proof is that `@www/core` now exports `SpotifyClient`.

## Points confirmed correct

- **Importer count: 4 files** — `apps/api/src/services/spotify-service.ts` (2 imports, lines 15-16), `__tests__/spotify-service.test.ts` (vi.mock L24 + 9 dynamic imports at 135/142/166/173/193/213/233/261/268), `__tests__/spotify-browse.test.ts` (vi.mock L23 + 3 dynamic at 158/165/212), `__tests__/spotify-client.test.ts` (L8, moves to core). Every line number in the plan's table matches the real files.
- **Non-importers correctly excluded** — `env.ts:77-79` (comment), `media.ts:123-154` (locally-defined `Spotify*Schema` Zod objects; the router imports `spotify-service`, not the integration), `env.test.ts:18-20` (comment). Verified.
- **Shim-vs-repoint decision is correct.** The barrel (`integrations/spotify/index.ts`) is a pure re-export with NO env-bound singleton — the env binding lives in `spotify-service.ts`'s lazy `getClient()`, which stays in apps/api unchanged. This is the media-path pattern, not the HA-shim pattern. Deleting the old dir and repointing (no shim) is right, and is what knip zero-tolerance demands.
- **Partial (`importOriginal`-spread) mock is mandatory and correctly reasoned.** `spotify-browse.test.ts` pulls the tRPC media router → `apps/api/src/db/index.ts` (`createPool` from `@www/core`) and `env.ts` (`databaseUrlFromSecret`/`hydrateSecretFiles` from `@www/core`) — verified those imports exist. A full `vi.mock("@www/core", () => ({...}))` would strip them. Spreading `importOriginal()` and overriding only `SpotifyClient`/`SpotifyError` preserves them. Note: today's tests already run `@www/core` fully real (they mock only `../integrations/spotify`), so `hydrateSecretFiles()` already executes at import in these tests — the partial mock preserves that exact behavior, no regression.
- **`mock`-prefixed factory vars** (`mockGetNowPlaying` etc.) already satisfy Vitest's hoisting allowance in the current tests; the retarget keeps the same names, so no `vi.hoisted` change is needed.
- **knip zero-tolerance** — `packages/core/package.json` maps `exports["."]` → `./src/index.ts`, so the barrel is a knip entry and its re-exported public types are the package's public API (not dead code). The `@public` JSDoc is carried verbatim. The current apps/api barrel already re-exports the same 5 types + 2 classes and passes knip; the core barrel inherits the same treatment (media-path/homeassistant precedent). No leftover shim or unused export is created.
- **Dependency declarations** — `packages/core/package.json` declares `@www/logger` (client's only external import); `apps/api/package.json` declares `@www/core`. No dep edits needed, as claimed.
- **Barrel insertion position** (`export * from "./spotify";` between `./secrets/hydrate` and `./unifi`) is alphabetically correct against the real `index.ts`.
- **Sub-barrel content** in the plan matches the real `integrations/spotify/index.ts` verbatim; **types.ts** (5 interfaces — plan Task-1 interface note says "6 interfaces" but the file has 5: `SpotifyCredentials`, `SpotifyNowPlaying`, `SpotifyRecentTrack`, `SpotifyPlaylistItem`, `SpotifyBrowseResult`; the sub-barrel `export type` block correctly lists all 5). Cosmetic count mismatch in prose only — the actual export list is correct, so no functional impact.
- **Verify chain** (`apps:gen` → `typecheck` → vitest → `apps:check` → `knip` → `lint`), explicit-path staging, `git show --stat HEAD` check, `pull --rebase --autostash`, backtick-free commit message — all present and correct per AGENTS.md discipline.
- **vitest auto-discovery** — `packages/core/vitest.config.ts` has no `include` and `src/media-path/index.test.ts` is auto-discovered, so `src/spotify/client.test.ts` will be too. The fallback (add `include`) is correctly noted as almost-certainly-unnecessary.
- **Commit message** `refactor(core): hoist Spotify client to @www/core (P1.3)` mirrors the landed P1.1 HA commit convention.
