# fastlane match: shared signing against the central certificates repo.
# One distribution cert for the whole account; this app gets its own profile.
git_url(ENV["MATCH_GIT_URL"] || "https://github.com/0x63616c/certificates.git")
storage_mode("git")
type("appstore")
app_identifier([ENV["BUNDLE_ID"]])
