# Captive Portal

Product boundary for guest WiFi onboarding.

The frontend app now lives in `products/captive-portal/apps/frontend`. The backend remains in `apps/api` until the M5 backend split moves it behind this product boundary.
