/**
 * Hardware source anchor UUIDs for the Groups modal's two always-present
 * floor cards (Desk · Line-In, Living Room · TV).
 *
 * Keep in sync with `DESK_LINE_IN_UUID` / `BEAM_UUID` in
 * products/control-center/api/src/services/sonos-sound-system-service.ts ,
 * the web cannot import api source, so these literals are duplicated here.
 */
export const DESK_LINE_IN_UUID = "RINCON_804AF28AAB2001400";
export const BEAM_UUID = "RINCON_74CA6093255801400";
