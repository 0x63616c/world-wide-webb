{
  "name": "@control-center/storybook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd ../../../products/control-center/web storybook",
    "build": "bun run --cwd ../../../products/control-center/web build-storybook",
    "test": "bun run --cwd ../../../products/control-center/web test",
    "typecheck": "bun run --cwd ../../../products/control-center/web typecheck"
  }
}
