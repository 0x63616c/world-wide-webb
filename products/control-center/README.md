# Control Center

Product boundary for the home dashboard, API, workers, Storybook host, Drizzle wrapper, map provisioner, and hosted iOS kiosk shell.

Runtime code intentionally remains in the top-level `apps/*` folders until the later M4 move tickets relocate it behind this product folder.
